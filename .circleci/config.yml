defaults: &defaults
  docker:
    - image: circleci/node:14-browsers
      environment:
        JOBS: 1
  working_directory: ~/site-aiki3

version: 2
jobs:
  checkout_code:
    <<: *defaults
    steps:
      - checkout
      - persist_to_workspace:
          root: .
          paths:
            - .

  install_dependencies:
    <<: *defaults
    steps:
      - attach_workspace:
          at: .
      - restore_cache:
          keys:
            - site-aiki3-node14-v1-{{ checksum "yarn.lock" }}
            - site-aiki3-node14-v1-
      - run:
          name: Yarn Install
          command: yarn install --non-interactive --pure-lockfile
      - save_cache:
          key: site-aiki3-node14-v1-{{ checksum "yarn.lock" }}
          paths:
            - ~/site-aiki3/node_modules
      - persist_to_workspace:
          root: .
          paths:
            - .

  test:
    <<: *defaults
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Run Tests
          command: yarn test

  build_deploy:
    <<: *defaults
    steps:
      - attach_workspace:
          at: .
      - run: sudo apt-get install ncftp
      - run:
          name: Build
          command: yarn build
      - run:
          name: Deploy
          command: ncftpput -R -v -u $FTP_USER -p $FTP_PASSWORD $FTP_HOST . dist/*

workflows:
  version: 2
  build_and_test:
    jobs:
      - checkout_code
      - install_dependencies:
          requires:
            - checkout_code
      - test:
          requires:
            - install_dependencies
      - build_deploy:
          requires:
            - test
          filters:
            branches:
              only: master
